//one can easily do the simple comparisions and the output will be in boolean
// some other kind of comparisons are as follows

console.log(null > 0)
console.log(null == 0)
console.log(null < 0)
console.log(null != 0)
console.log(null <= 0)
console.log(null >= 0)

console.log("2"=== 2) //it checks the data strictly and also checks the datatype and if not same then it will give the false
console.log("2"==2) // this will give true because the data type is not checked

 